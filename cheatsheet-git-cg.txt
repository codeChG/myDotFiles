
#### dotfiles config

alias dotfiles ="/usr/bin/git --git-dir=$HOME/.mydotfiles --work-tree=$HOME"
dotfiles config --local status.showUntrackedFiles no

git status      #aktueller Zustand des repos

git ls-files 		#listet alle getrackten files


git ls-files 		                    # listet alle getrackten file
git branch -va                      # listet alle aktiven (auch remote) branches
git checkout -b feature             # checkt neuen branch feature aus
git reset --soft HEAD~1             # macht letzten commit rueckgaengig

