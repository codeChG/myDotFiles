alias dotfiles ="/usr/bin/git --git-dir=$HOME/.mydotfiles --work-tree=$HOME"
dotfiles config --local status.showUntrackedFiles no

git status      #aktueller Zustand des repos

git ls-files 		#listet alle getrackten files
